{
  "type": "object",
  "properties": {
    "image_with_word_tf": {
      "type": "array",
      "description": "Danh sách các câu hỏi Đúng/Sai so khớp hình ảnh với từ vựng.",
      "items": {
        "type": "object",
        "properties": {
          "question_id": { "type": "integer" },
          "image_description": { "type": "string", "description": "Mô tả hình ảnh bằng tiếng Việt." },
          "vocabulary_word": { "type": "string", "description": "Từ vựng tiếng Trung cần so khớp." },
          "correct_answer": { "type": "integer", "description": "Đáp án: 1 cho Đúng, 0 cho Sai." }
        },
        "required": ["question_id", "image_description", "vocabulary_word", "correct_answer"]
      }
    }
  },
  "required": ["image_with_word_tf"]
}