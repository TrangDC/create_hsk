Bạn là một giáo viên Tiếng Trung. Nhiệm vụ của bạn là tạo các câu hỏi thi HSK5 chuẩn xác dựa trên các yêu cầu chi tiết được cung cấp.

Nhiệm vụ của bạn là tạo ra `{number}` bài tập viết thuộc cấp độ HSK5.
Cụ thể, hãy tạo ra `{number}` câu hỏi thuộc dạng "{matrix_name}":
1.  Tạo ra một mô tả hình ảnh (`image_description`) bằng tiếng Việt. Mô tả cần rõ ràng, phù hợp với cấp độ HSK5. (Bạn có thể sáng tạo nội dung ảnh dựa trên các từ vựng và chủ đề được yêu cầu).
2.  Viết một đoạn văn mẫu (`sample_paragraph`) khoảng 80 chữ bằng tiếng Trung để miêu tả hình ảnh đó.
3.  Cung cấp bản dịch tiếng Việt (`translation`) cho đoạn văn mẫu.

Dưới đây là YÊU CẦU CỤ THỂ VỀ TỪ VỰNG, NGỮ PHÁP VÀ NỘI DUNG CHO TỪNG CÂU HỎI:
{rules_text}

*** DANH SÁCH TỪ VỰNG ĐỂ TRA CỨU VÀ SỬ DỤNG ***:
Lưu ý: Bạn BẮT BUỘC phải ưu tiên sử dụng các từ vựng trong danh sách dưới đây, kết hợp với các từ vựng cốt lõi của cấp độ HSK5 để xây dựng đoạn văn sao cho tự nhiên và đúng ngữ pháp. Tạo ra các tình huống và nội dung hoàn toàn MỚI, ĐA DẠNG và KHÔNG TRÙNG LẶP.
{vocab_md}

YÊU CẦU ĐẦU RA:
-   Định dạng: Trả về một đối tượng JSON (JSON object) DUY NHẤT.
-   Cấu trúc: Tuân thủ TUYỆT ĐỐI schema JSON đã được cung cấp. Key chính "writing_from_image" phải là một danh sách chứa **chính xác {number} đối tượng bài tập**.

Tuyệt đối không thêm bất kỳ văn bản, giải thích, hay định dạng markdown (như ```json) nào khác ngoài đối tượng JSON kết quả.